# IaaS・PaaS・FaaS・EC2・Fargate まとめ

## 一番大事な違い

  項目           IaaS         PaaS         FaaS
  -------------- ------------ ------------ --------------
  サーバー       自分で管理   管理不要     管理不要
  OS             自分で管理   管理不要     管理不要
  ミドルウェア   自分で管理   管理不要     管理不要
  アプリ         自分で管理   自分で管理   関数だけ書く
  自由度         高い         中           低い

### 覚え方

-   **IaaS** = サーバー管理者になる
-   **PaaS** = アプリ開発者になる
-   **FaaS** = 関数だけを書く

------------------------------------------------------------------------

# IaaS

仮想マシン（VM）を借りるサービス。

例: - AWS EC2 - Google Compute Engine - Azure Virtual Machines

SSHでログインし、

-   OSアップデート
-   nginx
-   Docker
-   データベース
-   ランタイム

などを自分で管理する。

## OSアップデート

**ユーザーが行う。**

------------------------------------------------------------------------

# PaaS

アプリを動かす環境まで提供してくれる。

例: - Heroku - Render - Railway

ユーザーはコードをデプロイするだけ。

OS・サーバー・Webサーバー・再起動などはサービス側が管理する。

## OSアップデート

**サービス提供者が行う。**

ただし、Ruby・Node.js・Javaなどのランタイムのバージョンはユーザーが選択することが多い。

------------------------------------------------------------------------

# FaaS

関数単位でコードを実行する。

例: - AWS Lambda - Google Cloud Functions

イベントが来たときだけ実行される。

------------------------------------------------------------------------

# ECS と Fargate

## ECS

コンテナを管理するサービス。

実行基盤として **EC2** または **Fargate** を選択できる。

### ECS on EC2

-   EC2でコンテナを動かす
-   EC2のOSは自分で管理

→ IaaS寄り

### ECS on Fargate

-   Fargateでコンテナを動かす
-   ホストOSはAWSが管理

→ PaaS寄り

------------------------------------------------------------------------

# EC2 と Fargate の違い

## EC2

EC2では、自分のVM上でコンテナを動かす。

    EC2 (Ubuntu)
     ├─ Docker
     ├─ nginx
     └─ コンテナ

管理するもの:

-   OS
-   Docker
-   パッケージ
-   セキュリティパッチ
-   ディスク容量
-   サーバー障害対応

SSHでログインできる。

### スケール

EC2を何台用意するかを考える必要がある。

### 課金

VM単位で課金される。 VMがアイドル状態でも料金は発生する。

------------------------------------------------------------------------

## Fargate

サーバーは見えない。

    Fargate
     └─ コンテナ

管理するもの:

-   コンテナイメージ
-   CPU
-   メモリ
-   ネットワーク設定

OSやDockerはAWSが管理する。

SSHはできない。

### スケール

AWSが自動で適切なホスト上にコンテナを配置する。

### 課金

コンテナに割り当てたCPU・メモリと実行時間に応じて課金される。

------------------------------------------------------------------------

## Dockerはどこ？

Docker（正確にはコンテナランタイム）はAWS内部で動いている。

利用者は

-   コンテナイメージ
-   CPU
-   メモリ

だけ指定すればよく、コンテナを起動するホストはAWSが管理する。

------------------------------------------------------------------------

# 責任範囲

## IaaS

AWS: - VM

ユーザー: - OS - Docker - nginx - ランタイム - アプリ

## PaaS

サービス: - VM - OS - ランタイム実行環境 - デプロイ基盤

ユーザー: - アプリ - ライブラリ - 設定

## FaaS

サービス: - インフラ全体

ユーザー: - 関数

------------------------------------------------------------------------

# まとめ

-   **EC2** = VMを借りる（IaaS）
-   **Fargate** = サーバーレスなコンテナ実行基盤（PaaS寄り）
-   **Lambda** = 関数実行基盤（FaaS）
-   **ECS** = コンテナを管理するサービス。EC2またはFargate上で動作する。

EC2は「サーバーを管理する世界」、
Fargateは「コンテナだけを意識する世界」と覚えると整理しやすい。
