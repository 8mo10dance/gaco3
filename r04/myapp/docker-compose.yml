services:
  rails:
    build:
      context: .
      dockerfile: Dockerfile
    image: myapp:1.0.0
    tty: true
    stdin_open: true
    volumes:
      - .:/rails
      - bundle_rails60:/usr/local/bundle
    ports:
      - "3000:3000"

volumes:
  bundle_rails60:
